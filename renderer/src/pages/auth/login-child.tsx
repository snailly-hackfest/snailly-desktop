import React from 'react'

import { LoginChild } from '@/modules'
import { PageWrapper } from '@/layout'

const loginChild = () => {
    return (
        <PageWrapper layoutType='plain'>
            <LoginChild />
        </PageWrapper>
    )
}

export default loginChild
