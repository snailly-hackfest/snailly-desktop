import { css } from '@emotion/css'

import { theme } from '@/styles'

export const sLink = css`
    font-weight: ${theme.font.weight.bold};

    color: ${theme.color.secondary.lightOrange};

    text-decoration: none;
`
